# 小牛电商接口缺陷记录

- **被测系统**：小牛电商测试实战系统（http://43.133.227.52/api）
- **发现阶段**：用例设计与手工探测定级（详见 test_cases.md）
- **自动化状态**：全部缺陷已由 **@pytest.mark.xfail** 用例钉住，修复后 XPASS 可自动提醒
- **记录日期**：2026-09-22

## 缺陷汇总表

| 缺陷编号 | 标题 | 模块 | 严重级别 | 类型 | 关联用例 | 自动化用例 |
|---|---|---|---|---|---|---|
| XN-BUG-001 | quantity=0 时加购未校验且数量异常+1 | 购物车 | Major（严重） | 参数校验缺失 | add_cart_003 | test_add_cart_quantity_zero |
| XN-BUG-002 | quantity 无上限校验，可一次加入大量商品 | 购物车 | Normal（一般） | 参数校验缺失 | add_cart_005 | test_add_cart_quantity_too_large |
| XN-BUG-003 | 删除不存在的商品返回成功码 code=0 | 购物车 | Minor（轻微） | 状态码语义不准 | delete_cart_002 | test_delete_cart_nonexistent_product_id |
| XN-BUG-004 | 已支付订单可重复支付，返回 code=0 | 订单/支付 | Major（严重） | 业务逻辑缺陷 | pay_orders_004 | test_pay_order_duplicate |
| XN-BUG-005 | 已取消订单可重复取消，返回 code=0 | 订单/取消 | Minor（轻微） | 状态码语义不准 | cancel_orders_004 | test_cancel_order_cancelled_order |

---

## 通用复现前置条件

1. 登录获取 token：`POST /api/login`，body：`{"username":"tester","password":"123456"}`
2. 请求头携带：`Authorization: Bearer {token}`
3. 如需干净数据：管理员登录（admin/admin123）后 `POST /api/reset` 重置练习数据

---

## XN-BUG-001 加入购物车：quantity=0 无参数校验

- **接口**：`POST /api/cart`
- **请求体**：`{"productId": 101, "quantity": 0}`
- **复现步骤**：
  1. 查询购物车，记录商品 101 数量（或确认不存在）
  2. 以 `quantity=0` 调用加购接口
  3. 再次查询购物车
- **实际结果**：返回 `code=0`，商品数量比操作前 **+1**（被当成正常加 1 件处理）
- **预期结果**：拒绝请求，返回参数错误（数量必须大于 0）
- **复现频率**：稳定复现
- **影响分析**：请求参数与实际变更不一致，属于脏数据入口；下游订单金额计算存在被绕过校验的风险

## XN-BUG-002 加入购物车：quantity 无上限校验

- **接口**：`POST /api/cart`
- **请求体**：`{"productId": 101, "quantity": 100}`（自动化用例使用 999）
- **实际结果**：返回 `code=0`，购物车数量直接 +100
- **预期结果**：对单次加购数量做合理上限校验（如不得超过库存或单笔限制）
- **复现频率**：稳定复现
- **影响分析**：可一次性超出库存下单，压测/恶意场景下放大；正常业务无此交互

## XN-BUG-003 删除购物车：删除不存在的商品返回成功

- **接口**：`DELETE /api/cart/9999`
- **实际结果**：返回 `code=0` 及当前购物车列表
- **预期结果**：返回明确错误码（如 404/400），提示商品不在购物车
- **复现频率**：稳定复现
- **影响分析**：核心数据未受影响（购物车内容与操作前一致），主要问题是接口对异常操作返回状态不准确，前端若按 `code` 判断成功会产生误导提示
- **状态**：疑似问题，待与开发确认设计意图

## XN-BUG-004 支付订单：已支付订单可重复支付

- **接口**：`POST /api/orders/{orderId}/pay`
- **复现步骤**：
  1. 加购 → `POST /api/orders` 创建订单，记录 orderId
  2. 第一次支付：返回 `code=0`，状态"已支付"
  3. 对同一订单再次支付
- **实际结果**：第二次支付仍返回 `code=0` 和订单详情
- **预期结果**：拒绝重复支付，返回明确提示（如"订单已支付"，状态码 409）
- **复现频率**：稳定复现
- **影响分析**：支付接口不幂等，真实支付场景下存在**重复扣款资金风险**，建议优先修复

## XN-BUG-005 取消订单：已取消订单可重复取消

- **接口**：`POST /api/orders/{orderId}/cancel`
- **复现步骤**：创建订单 → 取消 → 对已取消订单再次取消
- **实际结果**：返回 `code=0` 和订单详情，状态保持"已取消"
- **预期结果**：取消失败并提示"订单已取消"
- **复现频率**：稳定复现
- **影响分析**：订单状态未被破坏，但返回码语义与业务事实不符
- **状态**：疑似问题，待与开发确认设计意图

---

## 缺陷修复后的联动机制

- 以上 5 条在 `api_test` 中以 `xfail + reason（含用例编号）` 标记；带 `strict=True` 的用例在缺陷修复后会以 **XPASS 失败**的形式暴露，提醒测试人员：
  1. 摘除对应用例的 xfail 标记
  2. 更新本记录状态为"已修复待验证 → 已关闭"
  3. 回归运行：`pytest api_test -k "quantity_zero or too_large or nonexistent_product or duplicate or cancelled_order" -v`
